/* Este código é o responsável por criar uma instância do axios com uma configuração padrão,
 * (no caso a baseURL) pra não ser necessário escrever essa URL toda vez que fizer
 * uma requisição para a API.
 */

// import axios from 'axios'

// const Api = axios.create({
//   baseURL: 'https://choose-acai/api/v1/'
// })

// export default Api
